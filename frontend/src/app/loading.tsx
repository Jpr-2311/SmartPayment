import { LoadingScreen } from '@/components/shared';

/**
 * Next.js loading UI displayed during route transitions.
 * Shows the full-screen animated loading screen.
 */
export default function Loading() {
  return <LoadingScreen />;
}
